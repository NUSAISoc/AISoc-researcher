# Research Question

There is exactly one main research question at a time. This file is its authoritative home. If it changes, the change is universal across `ProjectProposal.md`, `RESEARCH_RULES.md`, `.beryl/agent/project-brief.md`, `README.md`, `report/`, and every dependent file, in the same unit of work.

## The one main research question

> `<Your one main research question goes here.>`

Replace the placeholder above with your study's single main research question. It must read identically in every file the [Synchronization Contract](../.beryl/agent/synchronization-contract.md) names for the research question. The harness test suite fails if the canonical question is missing from any required file, so make the update universal in one unit of work.

## Overall research objective

> `<State the overall research objective that frames the question. This frames the question; it does not introduce a second main research question.>`

## Sub-questions

These decompose the main question; they do not compete with it.

- **RQ1.** `<first sub-question>`
- **RQ2.** `<second sub-question>`
- **RQ3.** `<third sub-question>`

## How this was derived

> `<Explain how you arrived at this single question from the literature gap (docs/01) and the problem analysis (docs/02). If it supersedes an earlier question, record the earlier version here as the audit trail and delete it from every other file.>`

## Why this is the right single question

> `<Argue that it is novel, testable in your setting, and grounded in the literature. Each component should map to a reading.>`
