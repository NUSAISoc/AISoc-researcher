# Solution Design

This file specifies the proposed intervention (the treatment) at the research-design level. It is the canonical owner of the solution design; keep `solution/PRD.md` and the proposal synchronized with it. No application code is written until the researcher green-lights `solution/PRD.md`.

Use `academic-research-writer` for this document. Apply the Systems Thinking methodology: the intervention is the leverage point identified in `docs/02-problem-analysis.md`.

## The intervention

> `<Describe the treatment: what it does differently from the control condition, and how each part maps to a mechanism supported by the literature review.>`

## What differs between conditions

> `<State exactly what differs between the treatment and the control condition, and what is held constant. Keep the comparison clean: only the mechanism under test should vary.>`

## Instruments (if any)

> `<If the solution scores participants or agents against a reference (a rubric, a reference diagram, an oracle), specify it here. This is solution-design instrument content, owned by this file.>`

## Design rationale

> `<Justify each design choice against a simpler alternative (Occam's razor). A new construct, arm, or measure must earn its place.>`
