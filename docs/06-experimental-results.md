# Experimental Results

This file holds the study's results. **It is empty of results until real data exists.** No experimental result, score, or statistic may be entered here unless it comes from real, user-provided data or from the sandboxed runner in `experiments/`, traced through `results/ledger.csv`. A placeholder number is a fabrication and the harness test suite will fail (see `tests/test_harness_standards.py::NoFabricatedDataTest`).

## Status

No data has been collected yet. When real data exists, summarise the findings here, generate figures from `results/` with the scripts in `analysis/`, and synchronize the report per the synchronization contract.

## Results

> `<Empty until real data exists. When it does: report the outcome metric by condition, the effect, and the uncertainty. Reference the ledger rows and result logs the numbers came from.>`
