# Product Requirements Document

This PRD specifies the solution application that implements the intervention designed in `docs/04-solution-design.md`. It is the bridge between the research design and a buildable system. It is not code, and no code is written until the researcher green-lights this document.

Keep this synchronized with `docs/04-solution-design.md` per the synchronization contract.

## Problem and goal

> `<What the application must do, tied to the research question and the intervention.>`

## Users and context

> `<Who uses it and in what setting.>`

## Requirements

> `<The functional requirements, each tied to a mechanism in the solution design. Keep them minimal (Occam's razor); a requirement that does not serve the research question does not belong here.>`

## Observability and human control

> `<What the application must log and expose so the researcher retains full observability, and how the researcher can change its behavior by hand or by prompt.>`

## Non-goals

> `<What is explicitly out of scope, to keep the build honest.>`

## Green-light

Status: **not green-lit.** When the researcher approves this PRD, record the approval here (dated), then the app is built as a submodule under `solution/`.
